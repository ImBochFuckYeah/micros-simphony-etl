# micros-simphony-etl
Automated integration service built with Node.js to fetch Oracle MICROS Simphony POS sales data (JSON) via SFTP, persist it in SQL Server, and sync it to SAP Business One via Service Layer.
